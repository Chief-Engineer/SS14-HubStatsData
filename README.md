Collects and displays stats for SS14 servers and hubs.

Servers not configured in `config.yaml` will still have stats collected, but will be displayed as "Unknown Server" to keep GitHub happy, since basically all hub data is untrusted user input.

PRs to add servers or established hubs are welcome.
